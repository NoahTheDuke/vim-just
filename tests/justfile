run FILTER='':
  cargo run {{FILTER}}

watch FILTER='':
  cargo watch \
    --clear \
    --debug \
    --exec "run {{FILTER}}" \
    --watch ..

install-rustup:
  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

deps:
  cargo install cargo-watch
