arch()
arch(foo)

os()
os(foo)

os_family()
os_family(foo)

invocation_directory()
invocation_directory(foo)

justfile()
justfile(foo)

justfile_directory()
justfile_directory(foo)

just_executable()
just_executable(foo)

env_var()
env_var(foo)

env_var_or_default()
env_var_or_default(foo)
env_var_or_default(foo, bar)
